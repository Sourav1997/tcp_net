# tcp_net
TCP_net

It is a simple server client program.
Here the server connects to the client with a socket enabling smooth communication.

Process:
execute : ./cient.c // this a client program
server  : ./server.c  // this is the server program

The code enables the server to communicate with a client at a time

when server : ./mserver1.c // this server is enabled to communicate with multiple clients as a time
